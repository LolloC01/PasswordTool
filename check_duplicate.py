def check_duplicate(password: str, to_check: list):
    """Controlla se la passsword è stata gia creata o se assomiglia troppo a un'altra

    Args:
        password (str): password da controllare
        to_check (list): lista delle password da controllare
    """